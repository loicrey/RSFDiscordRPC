{
  "name": "rsfdiscordrpc-web",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Presentation website and public asset registry for RSFDiscordRPC.",
  "scripts": {
    "test": "node src/worker.test.js",
    "dev": "wrangler dev",
    "devr": "wrangler dev --remote",
    "deploy": "wrangler deploy --env="
  },
  "devDependencies": {
    "wrangler": "^4.11.1"
  },
  "dependencies": {
    "@jsquash/jpeg": "^1.6.0",
    "@jsquash/resize": "^2.1.1"
  }
}
